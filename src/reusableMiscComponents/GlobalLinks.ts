/**
 * @GlobalLinks - Path string for any links in the
 * misc folder set of bot commands. Expand when it
 * is needed - ProBoz
 * 
 * NOTE: Do NOT place this directory inside of a
 * slappey generated folder otherwise you will have
 * a bad time.
 */

type GLOBAL_LINKS = {
    memesUrl: string,
};

export const GlobalLinks: GLOBAL_LINKS = {
    memesUrl: "/assets/memes/",
};