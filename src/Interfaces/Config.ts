/**
 * @export -
 * TokenConfig: Interface for Discord bot, include in .gitignore
 * PrefixConfig: Interface for Discord bot prefix.
 * 
 * Add more exported interfaces when needed.
 */

export interface TokenConfig {
    token: string;
};

export interface PrefixConfig {
    prefix: string;
};